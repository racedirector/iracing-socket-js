import { IRacingSocketConnectionContext } from "./context";

export const IRacingSocketConnectionConsumer =
  IRacingSocketConnectionContext.Consumer;

export default IRacingSocketConnectionConsumer;
